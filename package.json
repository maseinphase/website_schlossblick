{
  "name": "fewo-schlossblick",
  "version": "1.0.0",
  "description": "Website für die Ferienwohnung Schlossblick in Eisfeld mit Booking.com iCal-Kalendersynchronisation",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx wrangler pages deploy dist --project-name fewo-schlossblick",
    "push:github": "./scripts/sync-github.sh"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
